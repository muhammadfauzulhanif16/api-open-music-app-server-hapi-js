const { AlbumValidator } = require('./album')
const { SongValidator } = require('./song')
const { UserValidator } = require('./user')

module.exports = {
  AlbumValidator,
  SongValidator,
  UserValidator
}
